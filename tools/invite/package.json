{
  "name": "@conversant/invite-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "conversant-invite": "./bin/conversant-invite.cjs"
  },
  "scripts": {
    "generate": "tsx src/cli.ts",
    "lint": "biome check",
    "format": "biome format --write",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@conversant/backend-data": "workspace:*",
    "@conversant/config": "workspace:*",
    "commander": "^14.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.4",
    "@types/node": "^25",
    "tsx": "^4.20.6",
    "typescript": "^5"
  }
}
