{
  "name": "conversant",
  "private": true,
  "packageManager": "yarn@4.9.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "db:generate": "yarn workspace @conversant/backend-data prisma:generate",
    "db:migrate:dev": "yarn workspace @conversant/backend-data prisma:migrate:dev",
    "db:migrate:deploy": "yarn workspace @conversant/backend-data prisma:migrate:deploy",
    "db:studio": "yarn workspace @conversant/backend-data prisma:studio"
  },
  "devDependencies": {
    "turbo": "^2.8.12",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  }
}
